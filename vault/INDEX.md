# INDEX — the router

Read this at session start. Do not bulk-load the vault: find the topic
below, open its one home, and read only what the task needs. This file is
the only note that is always in context, so it stays small.

| If the task involves… | Open |
|---|---|
| Who the user is and how they like things done | [[user-preferences]] |
| A ruling already made (never re-ask a settled question) | [[decisions]] |
| The state of ongoing work | [[example-project]] |
| A person the user works with | [[example-collaborator]] |
| What happened on a given day | the note for that date in `daily/` |
| Resuming the previous session | [[LATEST-SESSION]], read in full |

## Rules of the road

The owner of these rules is [[OPERATING-CONTRACT]]; the lines below are
the always-loaded summary, and the contract wins if they ever disagree.

- One home per topic. Update the owner file; never copy a fact into a
  second note.
- Newest wins on any conflict. Fix or retire the stale line; never keep
  both versions.
- Write back what you learn: durable facts go to the owner file plus
  today's daily note, same session.
- Checkpoint before you run out of context: rewrite [[LATEST-SESSION]]
  so the next session can resume at full depth.

The full read/write contract lives in [[OPERATING-CONTRACT]].
